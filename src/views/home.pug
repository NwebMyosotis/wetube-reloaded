extends base
include mixins/video

block content
    div#video-area
        each video in videos
            +videoMixin(video)
            +videoMixin(video)
            +videoMixin(video)
            +videoMixin(video)
            +videoMixin(video)
            +videoMixin(video)

        else 
            li Sorry Nothing Found.